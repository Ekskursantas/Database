Coded in Python. So, before running the code install Python. 
The assignement was not finished.
The script is writing in txt file and encodes ASCII to Binary.
Missing on indexing and storing a hashmap locally.


To edit that values you have to go inside the script and edit "write_to('emilisapoop', 97)". Everytime the function is envoced the new string is appended. So the binary file is always growing.
